name: SAST

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  codeql:
    name: CodeQL
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [javascript-typescript]

    steps:
      - uses: actions/checkout@v4.2.2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@codeql-bundle-v2.25.2
        with:
          languages: ${{ matrix.language }}
          queries: security-extended

      - uses: oven-sh/setup-bun@v2.2.0
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Autobuild
        uses: github/codeql-action/autobuild@codeql-bundle-v2.25.2

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@codeql-bundle-v2.25.2
        with:
          category: /language:${{ matrix.language }}
          upload: true
